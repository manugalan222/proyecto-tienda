<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.text.Text?>

<BorderPane prefHeight="480.0" prefWidth="800.0" stylesheets="@estilos.css"
            xmlns="http://javafx.com/javafx/24.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
                fx:controller="com.galan.proyectotienda2025.VentaControlador">

    <!-- Barra lateral -->
    <left>
        <VBox id="barra-lateral" spacing="10.0" prefWidth="300.0" styleClass="sidebar" >
            <children>
                <Button text="Inicio" styleClass="menu-btn" maxWidth="Infinity" onAction="#onInicioButtonClick"/>
                <Button text="Productos" styleClass="menu-btn" maxWidth="Infinity" onAction="#onProductosButtonClick"/>
                <Button text="Ventas" styleClass="menu-btn" maxWidth="Infinity"/>
                <Button text="Clientes" styleClass="menu-btn" maxWidth="Infinity" onAction="#onClienteButtonClick"/>
                <Button text="Inventario" styleClass="menu-btn" maxWidth="Infinity" onAction="#onInventarioButtonClick"/>
                <Button text="Corte" styleClass="menu-btn" maxWidth="Infinity"/>
                <Button text="Configuración" styleClass="menu-btn" maxWidth="Infinity"/>
            </children>
        </VBox>
    </left>

    <!-- Contenido principal -->
    <center>
        <VBox spacing="20.0" styleClass="main-content" alignment="TOP_CENTER" style="-fx-padding: 20;">
            <children>

                <Text text="Registro de Venta" styleClass="titulo" />

                <!-- Entrada de producto -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <TextField fx:id="txtProductoId" promptText="ID del Producto" prefWidth="200"/>
                    <Button text="Añadir Producto" />
                </HBox>

                <!-- Tabla de productos añadidos -->
                <TableView fx:id="tablaProductos" prefHeight="200" prefWidth="Infinity">
                    <columns>
                        <TableColumn fx:id="columnaId" text="ID" prefWidth="100"/>
                        <TableColumn fx:id="columnaNombre" text="Producto" prefWidth="200"/>
                        <TableColumn fx:id="columnaPrecio" text="Precio" prefWidth="100"/>
                        <TableColumn fx:id="columnaCantidad" text="Cantidad" prefWidth="100"/>
                        <TableColumn fx:id="columnaSubtotal" text="Subtotal" prefWidth="100"/>
                    </columns>
                </TableView>

                <!-- Método de pago -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="Método de Pago:"/>
                    <ComboBox fx:id="comboMetodoPago" prefWidth="200"/>
                </HBox>

                <!-- Descuento -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="Descuento (%):"/>
                    <TextField fx:id="txtDescuento" promptText="0" prefWidth="100"/>
                </HBox>

                <!-- Precio final -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="Precio Final:"/>
                    <TextField fx:id="txtPrecioFinal" editable="false" prefWidth="150"/>
                </HBox>

                <!-- Botones -->
                <HBox spacing="10" alignment="CENTER_RIGHT">
                    <Button text="Finalizar Venta" styleClass="primary-btn"/>
                    <Button text="Cancelar" styleClass="danger-btn"/>
                </HBox>

            </children>
        </VBox>
    </center>
</BorderPane>
